# Repository review — 2026-09-20

Reviewed `cb1c54e`, all 91 tracked files in the current checkout, the complete pre-existing uncommitted diff, and untracked additions. No commit, publication, push, data reset, production-app restart, or permission reset was performed. The original working-tree diff is retained at `/tmp/socius-review-start.diff`.

## Recorded failures and scope

The user's Desktop recordings (12.38.39 AM drag and 12.38.49 AM Snippets) were successfully decoded and inspected. The earlier temporary Screenshot-folder copies were inaccessible. Direct external UI automation is still unavailable: the host reports Accessibility and Screen Recording denied (`AX -25211`). User-operated interaction with the diagnostic development app supplied the missing real-app evidence.

### Drag: confirmed controller clamp, corrected locally

[Sources/Socius/EdgeDockController.swift:173](/Users/nmashchenko/Documents/socius/Sources/Socius/EdgeDockController.swift:173) clamped Y to `screen.visibleFrame.minY - feetFromBottom`. On the actual primary display, the full frame is `(0,0,2560,1440)` and the visible frame begins at Y=74. During the user's drag, pointer Y reached 0.0195, requested panel Y reached -140, the controller clamped it to 20, and AppKit's resulting panel Y was exactly 20. Scale was 1.08, the footer below the feet was 54 points, and the real input region was attached. This identifies the clamp during dragging rather than a coordinate-conversion or native-window-constraint failure.

Evidence: `/tmp/socius-drag-before.log` (captured from the running development app), representative timestamp `1789879647.397891`. The pet's feet were held at 20 + 54 = 74 points above the display bottom.

The lower bound now uses the physical display bottom. [PetAppearance.swift:6](/Users/nmashchenko/Documents/socius/Sources/Socius/PetAppearance.swift:6) supplies movement bounds that still keep the head below the menu bar. Resize, summon and idle-return geometry use the same bounds, so release or later travel does not restore the old restriction. A new test dispatches events through the full `DesktopPetView`/`PetPanel` tree on both connected displays, including release and idle/return. It passed in the final full run. The user then dragged the rebuilt application: the live trace reaches actual panel Y=-54 with a 54-point footer (feet at display Y=0), compared with Y=20 before the fix. Evidence: `/tmp/socius-drag-after.log`, timestamp `1789880647.497513`. The user moved upward before releasing that gesture, so holding a bottom position through release/idle is established by the full-host regression rather than claimed as a separate live observation.

### Snippets: upstream parity requested instead of further failure tracing

The recording establishes that controls reveal while the requested reorder does not visibly occur. The user subsequently directed: “for snippets ignore lets just copy same functionality / space that cyclop has or even our clipboard provides (icons on hover).” Further intermittent-failure tracing was therefore superseded by restoring the upstream interaction/layout.

[Vendor/Cyclop/Sources/UI/SnippetsPane.swift:217](/Users/nmashchenko/Documents/socius/Vendor/Cyclop/Sources/UI/SnippetsPane.swift:217) now uses Cyclop's 26-point display rows, 3-point list spacing, 9-point horizontal padding/spacing, trailing Spacer, plain 9-point up/down/delete icons, hover-only reveal, row surface highlight, and 0.16-second ease-out animations. Whole-row copy and double-click edit follow the imported source. Socius retains the requested shared 34-point native input height and Return/Escape handling; editing rows expand to fit those fields.

[SnippetInput.swift:25](/Users/nmashchenko/Documents/socius/Vendor/Cyclop/Sources/UI/SnippetInput.swift:25) requests native focus on a focus transition rather than on every SwiftUI update, and row fields share an editing group so moving between Name and Text does not prematurely commit. Leaving a row commits its edit. The forced-hover fixture was removed: it required an internal state seam and did not exercise actual hover/event routing. Native input delegate tests and upstream store regressions remain. No claim is made that the historical intermittent failure was independently reproduced and eliminated.

Reference: [Cyclop Snippets source](https://github.com/akalikbergenov/cyclop/blob/8d9ea04672898f651af2794d9be6289eebc658a1/Sources/Cyclop/UI/SnippetsPane.swift). The main-branch source was also inspected; the imported checkout is `/tmp/socius-cyclop-reference`.

## Material review findings fixed locally

| Priority | Finding and source | Fix and evidence |
| --- | --- | --- |
| P1 | [Services/NoteStore.swift:107](/Users/nmashchenko/Documents/socius/Vendor/Cyclop/Sources/Services/NoteStore.swift:107) treated unreadable JSON as an empty store; opening/leaving Notes could overwrite it. | Explicit read-failure state blocks mutation; file bytes are checked before saving to protect external changes. [UI/NotesPane.swift:24](/Users/nmashchenko/Documents/socius/Vendor/Cyclop/Sources/UI/NotesPane.swift:24) exposes read/write errors. Unchanged-source disposable-file probe reproduced data loss; new regression preserves the file and unsaved in-memory edits. |
| P1 | [Services/SnippetStore.swift:150](/Users/nmashchenko/Documents/socius/Vendor/Cyclop/Sources/Services/SnippetStore.swift:150) reordered cached data without reloading, erasing external additions or malformed contents. | Relative arrow movement now applies to a freshly read list and refuses broken files. Disposable-file probe reproduced both failures; regression covers both. |
| P2 | [Services/ClipboardStore.swift:71](/Users/nmashchenko/Documents/socius/Vendor/Cyclop/Sources/Services/ClipboardStore.swift:71) could collect copies made while paused and deliver a pending image after pause. | Resume snapshots the change count; pause/clear invalidate delayed callbacks. Both failures reproduced on a private pasteboard; both regressions pass without using the user's clipboard. |
| P2 | [PrivacyMode.swift:96](/Users/nmashchenko/Documents/socius/Vendor/Cyclop/Sources/Model/PrivacyMode.swift:96) had no caller; revealed private rows survived pocket closure. | [PocketAdapter.swift:71](/Users/nmashchenko/Documents/socius/Vendor/Cyclop/Sources/PocketAdapter.swift:71) and [AnchoredPocket.swift:113](/Users/nmashchenko/Documents/socius/Sources/Socius/AnchoredPocket.swift:113) reset reveals at dismissal/disappearance. Regression covers a revealed snippet becoming covered again. |
| P2 | [Sources/Socius/UsageService.swift:55](/Users/nmashchenko/Documents/socius/Sources/Socius/UsageService.swift:55) cancelled a silent Codex process only after its timeout. | Cancellable stdout stream and process cleanup end the request promptly; timeout has a distinct UI error. Original client took 2.89 seconds after cancellation with a 3-second timeout; regression now finishes its entire cancellation case in about 0.21 seconds, including the initial 0.2-second wait. A handshake regression verifies successful connections after cancellation/timeout. |
| P2 | [Services/ScreenshotVault.swift:12](/Users/nmashchenko/Documents/socius/Vendor/Cyclop/Sources/Services/ScreenshotVault.swift:12) stored development/preview images in the production screenshot folder. | Development/tests/previews now save inside their isolated Support folder. Existing production images and shelf references are untouched. Path regression passes. |
| P2 | [Services/ScreenshotFolderWatcher.swift:117](/Users/nmashchenko/Documents/socius/Vendor/Cyclop/Sources/Services/ScreenshotFolderWatcher.swift:117) could keep “watching” an obsolete directory inode and remember deleted filenames forever. | Generation-gated events stop on rename/delete/revoke; removed names leave the seen set. Actual temporary-directory rename and delete/recreate regressions pass. |
| P2 | [PocketAdapter.swift:71](/Users/nmashchenko/Documents/socius/Vendor/Cyclop/Sources/PocketAdapter.swift:71) started Music at launch, allowing the fallback to request Automation access before tool use. | Media starts on first Music presentation. `MediaController.swift` makes start idempotent; `NowPlayingFeed.swift` clears EOF handlers, cancels pending restarts, rejects stale process callbacks, and closes owned streams on stop. Real Spotify playback and permission prompting remain manual checks. |
| P2 | [PlaygroundView.swift:167](/Users/nmashchenko/Documents/socius/Sources/Socius/PlaygroundView.swift:167) exposed a size slider but rendered a fixed-size pet. | Superseded by the user’s subsequent request: the separate preview was removed. The developer dashboard now controls the desktop model and controller directly. |
| P2 | [ToolStore.swift:65](/Users/nmashchenko/Documents/socius/Sources/Socius/ToolStore.swift:65) reported completion even for an initial false-to-false observation. | Only an actual state transition publishes completion. The full native suite caught a pocket reopening during drag dismissal; the targeted regression now passes. |
| P2 | [PetHotkey.swift:16](/Users/nmashchenko/Documents/socius/Sources/Socius/PetHotkey.swift:16) ignored handler installation failure and accepted unrelated Carbon hotkey events. | Check the installation result and the event's hotkey signature/ID; preserve existing registration rollback. |
| P2 | AI connection diagnostics were incomplete. | [UsageDiagnostics.swift:10](/Users/nmashchenko/Documents/socius/Sources/Socius/UsageDiagnostics.swift:10) records bounded connection-stage events to unified logging and exposes Copy connection diagnostics in AI Credits. Logs contain no provider output, account identifiers, tokens, auth-file contents, or executable paths. Raw CLI stderr is deliberately not retained. |
| P3 | [WelcomeView.swift:40](/Users/nmashchenko/Documents/socius/Sources/Socius/WelcomeView.swift:40) kept the walk timeline active after arrival. | Walking stops when the greeting appears, retaining the existing exact-once onboarding session and desktop suspension behavior. No measured CPU improvement is claimed. |
| P2 | CI ran native suites in parallel despite shared app-wide window state. | Build/release workflows and documented full-suite commands now use `--no-parallel`. No workflow was dispatched. |

Paths beginning with `Services/`, `UI/`, or `Model/` above are under `Vendor/Cyclop/Sources/`.

## Verification

- Baseline before this review's fixes: **108 tests in 22 suites passed**, `/tmp/socius-review-baseline-tests.log`. This did not prove either reported UI issue resolved.
- New persistence/clipboard/privacy/cancellation regressions: 8 passed, `/tmp/socius-review-regressions.log`; watcher regressions also passed in `/tmp/socius-review-regressions-2.log`.
- First full run: 118 tests, three assertions failed across two tests. This caught the pocket completion bug and a Codex RPC pipe issue. These were addressed, not waived. Targeted Codex retry run: 3 passed, `/tmp/socius-review-codex-recheck.log`; targeted pocket-dismissal run passed in `/tmp/socius-review-recheck.log`.
- Final full suite: **118 tests in 23 suites passed**, in 59.108 seconds, `/tmp/socius-review-final-tests.log`. Development bundle rebuilt and restarted successfully; `/tmp/socius-review-final-build.log`. Signature verification passed with access to the local trust settings. Bundle ID remains `app.socius.desktop.development`, channel `development`. Post-fix user-operated dragging is confirmed by the live trace (`/tmp/socius-drag-after.log`). The temporary trace was then disabled by restarting Dev normally with `--developer`; the diagnostic file remains available for review.
- All original upstream SHA-256 entries match the imported checkout; Objective-C helper is unchanged. The integration patch has been regenerated for all 17 modified imported Swift files; reconstruction was verified against every imported file after regeneration.
- Three opt-in tests were skipped: native file-picker cancellation and live Claude/Codex account connections.
- An isolated Snippets preview was rendered from the built executable and visually inspected (`build/tool-preview.png`). This is visual layout evidence, not hover/click verification.
- Shell syntax, plist validation, and diff whitespace checks passed during review. The tracked icon decoded and was visually inspected.

## Remaining verification limits

The following are real-app acceptance checks, not established by passing model/native-fixture tests: post-release/idle behavior at the bottom in a user-operated session; Snippets hover, first click, copy/edit/reorder under a filter; pocket spacing at both edges through the full size range; reminder clipping near the top edge; playground control feedback; repeated onboarding hide/reopen; Spotify transport/permission flows; and motion CPU cost. No claim of measured performance or complete UI parity is made.

The original review identified a race between filtering Spotify snapshots and sending commands to the system player. The follow-up now addresses transport commands directly to Spotify through its scripting interface; the helper’s private command protocol is no longer used by the Music controller.

## Follow-up changes after the initial review

- Spotify: the actual bundled helper returned a paused Arc session (PID 24611) while Spotify (PID 848) was running. Filtering that session previously cleared Music. `MediaController` now reads Spotify directly in this case, deduplicates reads and rejects replies after closing the pocket or receiving a newer Spotify snapshot. `PlayerBridge` propagates Automation denial without logging script output; Music shows the access error and Retry. Transport buttons address Spotify explicitly.
- Onboarding: ending the entrance walk left `idleMotion` disabled. `WelcomeView` now enables idle tentacle motion after arrival. The native regression waits for the real greeting task, samples the rendered tentacles across four later frames, and respects the host’s Reduce Motion setting.
- Fullscreen: `DesktopVisibilityController` observes macOS presentation mode and active-space/application changes, with a one-second public window-bounds check. The live test showed that this background app received presentation options = 0 during another app’s native fullscreen session; the geometry fallback closes that gap and handles visible fullscreen windows on other displays. Fullscreen and immersive menu/Dock hiding suppress pet and pocket, cancel movement, and block late open/file-picker requests. Leaving fullscreen restores the pet in place, with the pocket closed. The introduction also stays out of other apps’ fullscreen Spaces. No Accessibility or Screen Recording permission is requested by this feature.
- Dashboard and idle: the user explicitly removed the separate preview concept. The dashboard now receives the desktop’s `PetModel` and `EdgeDockController`; there is no second rendered pet or `PlaygroundPresence`. Mood, care, size, colors, timer presets and simulations act on the desktop pet. Normal timing is now 10 seconds idle / 30 seconds between peeks; Demo remains 5s/5s. Other apps’ mouse buttons and a stationary/stale hover no longer continually reset idle. Native full-host regression dispatches drag/release and verifies retreat at the new deadline.
- Branding: inspected runtime logs and visible strings across Sources, Vendor sources/helper, Scripts, resources, tests and workflows. Replaced all eight imported `NSLog("Cyclop: …")` prefixes with `Socius:`. Kept MIT attribution, import/module names, existing storage/preferences/pasteboard identifiers and bundled helper filename for compatibility.

Final follow-up suite: **126 tests in 26 suites passed**, 65.394 seconds, `/tmp/socius-beta4-final-tests.log`. The opt-in file-picker, Claude-account and Codex-account checks remain skipped. Native onboarding pixel comparison passes after waiting through the real entrance; its initial failures were a test color-threshold error, corrected after inspecting the captured frame and actual color values.

Actual Dev Music read: `/tmp/socius-spotify-live-verified.log`, timestamp `1789882407.047147`, reports `track=true playing=true` while the helper reports another app’s system session. No track titles or command output are logged. Playback transport itself was not automated.

Actual Dev fullscreen round trip: `/tmp/socius-fullscreen-live-verified.log`; suppression at `1789882724.5366611` reports `petVisible=false visibleChildren=0`, and restoration at `1789882730.522925` reports `petVisible=true visibleChildren=0`, both with panel frame `(2287,126,216,243)`. The app then entered idle at `1789882740.599755`, ten seconds after return. A second live check on the portrait display `(-1440,-862,1440,2560)` kept the pet hidden while focus moved to an app on the other display, then restored it after fullscreen exit (`/tmp/socius-fullscreen-secondary-verified.log`, `/tmp/socius-fullscreen-secondary-fixture.log`). Temporary tracing was disabled afterward. A separate native test owns the full desktop/pocket hosting tree and checks late open/file-picker requests, unchanged care state and onboarding handoff.

The first beta 4 GitHub CI run caught a native test that hardcoded parent X=500 and expected right-side attachment. On the runner’s smaller display it correctly attached left, so changing width should leave its X fixed. The test now positions its parent relative to the display and checks both attachment sides; no product code was changed for this correction. A subsequent hosted run exposed asynchronous redraw timing and a difference between NSWorkspace and SwiftUI Reduce Motion settings. Dismissal now waits for child removal within a bounded deadline; the visual test waits for a visible, settled head and reads the same SwiftUI environment as the pet. Both corrected native suites pass locally (`/tmp/socius-beta4-ui-readiness-tests.log`).

The vendor integration patch reconstructs all 24 manifest files exactly (18 modified). Shell syntax and whitespace checks pass. All new and modified source, tests and release configuration were reviewed again before committing. On the user’s later explicit instruction, this complete batch is prepared for commit and publication as `v0.1.0-beta.4` (build 4). The initial review’s no-publication constraint is superseded by that instruction.

## Reviewed file inventory

All tracked source, tests, scripts, workflows, manifests, docs, licensing, and assets were included. Binary icon review used decoding/visual inspection; the large integration patch was validated by reconstruction against imported source rather than treated as an independent implementation.

- `.github/workflows/build.yml`
- `.github/workflows/release.yml`
- `.gitignore`
- `Package.swift`
- `README.md`
- `Resources/AppIcon.icns`
- `Resources/Info.plist`
- `Resources/Release.entitlements`
- `Scripts/bundle.sh`
- `Scripts/notarize.sh`
- `Scripts/release-dmg.sh`
- `Scripts/release-version`
- `Scripts/release.sh`
- `Scripts/setup-local-signing.sh`
- `Sources/Socius/AnchoredPocket.swift`
- `Sources/Socius/ClaudeAccountUsage.swift`
- `Sources/Socius/ClaudeCLIUsage.swift`
- `Sources/Socius/CreatureView.swift`
- `Sources/Socius/DeveloperRuntimeView.swift`
- `Sources/Socius/EdgeDockController.swift`
- `Sources/Socius/PetFlight.swift`
- `Sources/Socius/PetHotkey.swift`
- `Sources/Socius/PetInteractionView.swift`
- `Sources/Socius/PetModel.swift`
- `Sources/Socius/PetPointerInput.swift`
- `Sources/Socius/PetSettingsView.swift`
- `Sources/Socius/PlaygroundView.swift`
- `Sources/Socius/PocketControls.swift`
- `Sources/Socius/PocketMotion.swift`
- `Sources/Socius/PocketScrollGutterFix.swift`
- `Sources/Socius/SociusApp.swift`
- `Sources/Socius/SociusIconView.swift`
- `Sources/Socius/ToolStore.swift`
- `Sources/Socius/ToolViews.swift`
- `Sources/Socius/UsageService.swift`
- `Sources/Socius/WelcomeView.swift`
- `Sources/Socius/WindowLayouts.swift`
- `Tests/CyclopToolsTests/PocketAdapterTests.swift`
- `Tests/CyclopToolsTests/PocketNoteEditorTests.swift`
- `Tests/CyclopToolsTests/ScreenshotAccessTests.swift`
- `Tests/CyclopToolsTests/SnippetStoreTests.swift`
- `Tests/SociusTests/ClaudeAccountUsageTests.swift`
- `Tests/SociusTests/ClaudeCLIUsageTests.swift`
- `Tests/SociusTests/EdgeDockTests.swift`
- `Tests/SociusTests/LayoutPermissionTests.swift`
- `Tests/SociusTests/PetFlightTests.swift`
- `Tests/SociusTests/PetInteractionTests.swift`
- `Tests/SociusTests/PetPointerTests.swift`
- `Tests/SociusTests/PetPreferencesTests.swift`
- `Tests/SociusTests/PlaygroundTests.swift`
- `Tests/SociusTests/PocketMotionTests.swift`
- `Tests/SociusTests/ToolIntegrationTests.swift`
- `Tests/SociusTests/ToolStoreTests.swift`
- `Vendor/Cyclop/LICENSE`
- `Vendor/Cyclop/README.md`
- `Vendor/Cyclop/Sources/App/Strings.swift`
- `Vendor/Cyclop/Sources/Model/PrivacyMode.swift`
- `Vendor/Cyclop/Sources/PocketAdapter.swift`
- `Vendor/Cyclop/Sources/Services/ClipboardStore.swift`
- `Vendor/Cyclop/Sources/Services/ConfigStore.swift`
- `Vendor/Cyclop/Sources/Services/DebouncedWrite.swift`
- `Vendor/Cyclop/Sources/Services/MediaController.swift`
- `Vendor/Cyclop/Sources/Services/NoteStore.swift`
- `Vendor/Cyclop/Sources/Services/NowPlayingFeed.swift`
- `Vendor/Cyclop/Sources/Services/PlayerBridge.swift`
- `Vendor/Cyclop/Sources/Services/ScreenshotFolderWatcher.swift`
- `Vendor/Cyclop/Sources/Services/ScreenshotVault.swift`
- `Vendor/Cyclop/Sources/Services/ShelfStore.swift`
- `Vendor/Cyclop/Sources/Services/SnippetStore.swift`
- `Vendor/Cyclop/Sources/Services/Support.swift`
- `Vendor/Cyclop/Sources/UI/ClipboardPane.swift`
- `Vendor/Cyclop/Sources/UI/Confirmation.swift`
- `Vendor/Cyclop/Sources/UI/MediaPane.swift`
- `Vendor/Cyclop/Sources/UI/NotesPane.swift`
- `Vendor/Cyclop/Sources/UI/PocketNoteEditor.swift`
- `Vendor/Cyclop/Sources/UI/ShelfDragSource.swift`
- `Vendor/Cyclop/Sources/UI/ShelfPane.swift`
- `Vendor/Cyclop/Sources/UI/Skeleton.swift`
- `Vendor/Cyclop/Sources/UI/SnippetInput.swift`
- `Vendor/Cyclop/Sources/UI/SnippetsPane.swift`
- `Vendor/Cyclop/Sources/UI/SpoilerField.swift`
- `Vendor/Cyclop/Sources/UI/Theme.swift`
- `Vendor/Cyclop/helper.m`
- `Vendor/Cyclop/integration.patch`
- `Vendor/Cyclop/upstream.json`
- `docs/interaction-design.md`
- `docs/releases/0.1.0-beta.1.md`
- `docs/releases/0.1.0-beta.2.md`
- `docs/releases/0.1.0-beta.3.md`
- `docs/releasing.md`
- `skills-lock.json`

### Untracked additions reviewed or introduced during this review

- `Sources/Socius/CLIExecutableLocator.swift`
- `Sources/Socius/PetAppearance.swift`
- `Sources/Socius/UsageDiagnostics.swift`
- `Sources/Socius/WelcomeWindowSession.swift`
- `Tests/CyclopToolsTests/ReviewRegressionTests.swift`
- `Tests/CyclopToolsTests/SnippetInputTests.swift`
- `Tests/SociusTests/CLIExecutableLocatorTests.swift`
- `Tests/SociusTests/CodexUsageClientTests.swift`
- `Tests/SociusTests/PetAppearanceTests.swift`
- `Tests/SociusTests/PetHotkeyTests.swift`
- `Tests/SociusTests/WelcomeWindowSessionTests.swift`
- `Vendor/Cyclop/Sources/Services/InteractionTrace.swift`
- `docs/agent-handoff.md`
